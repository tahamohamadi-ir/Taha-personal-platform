# P0-G0: no project environment variable has been approved yet.
#
# Add a variable only when its owning service, environment, purpose and secret
# handling rule are recorded in PROJECT_MANIFEST.md and a relevant ADR.
# Never place a real secret, token, password, OAuth refresh token or SSH key here.
