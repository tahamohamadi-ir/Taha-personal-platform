name: Admin frontend CI

# Builds the custom admin SPA (ADR-0026, apps/cms/admin-frontend/) whenever it
# changes. The SPA is served by Django at /admin/ (ADM-1 cutover, DEFER-0023
# CLOSED); this job keeps it buildable and type-safe.

on:
  push:
    branches: [main]
    paths:
      - "apps/cms/admin-frontend/**"
  pull_request:
    paths:
      - "apps/cms/admin-frontend/**"

permissions:
  contents: read

jobs:
  build:
    name: Build admin SPA
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/cms/admin-frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Set up Node
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/cms/admin-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Build
        run: npm run build

      - name: Whitespace check
        run: git diff --check HEAD
