# Risk Register

> ریسک‌های Medium/High، blockerها و ریسک‌های عملیاتی اینجا ثبت می‌شوند. مورد Low یا آزمون انجام‌نشده در `deferred-validation.md` می‌رود.

| ID | Phase/Slice | Severity | Description | Why deferred | Owner | Target phase/date | Mitigation/fallback | Evidence/command | Status |
|---|---|---|---|---|---|---|---|---|---|
| RISK-0001 | P0-G0 | High | P0-G0 در 2026-08-14 به‌صورت `PASS for static-only P1` ثبت شد: تصمیم مالک (پذیرش محدود `RISK-0003`، content pack proposal، text-mark + فونت حداقلی) و freeze فنی R2 مستند شده‌اند. PASS کلی CMS اعلام نشده و scope فقط static P1 است. | شرایط gate برای first live ایستا برآورده شد؛ scaffold محدود به static P1 مجاز است. | Project owner | Closed 2026-08-14 | scaffold فقط برای static P1؛ CMS/DB/contact persistence همچنان به gateهای P3 وابسته‌اند. | LOG-0051 تا LOG-0054؛ تصمیم مالک 2026-08-14. | CLOSED |
| RISK-0002 | P0-G0 / Server access | High | یک secret مربوط به دسترسی سرور در تصویرِ گفت‌وگو قابل مشاهده شد؛ خود secret در repository و ledger ثبت نشده است. مالک تأیید کرد credential مذکور را خارج از گفت‌وگو rotate کرده است؛ حساب non-root با کلید جدید وارد می‌شود، sudo آن تأیید شده و effective SSH policy root/password/key-interactive login را غیرفعال نشان می‌دهد. | rotation مستقل قابل مشاهدهٔ مقدار secret نیست، اما attestation مالک همراه با key-only access و evidence policy، remediation موردنیاز را فراهم می‌کند. | Project owner | Closed 2026-08-14 | credentialها فقط در password manager باقی بمانند؛ هر exposure بعدی این risk را دوباره باز می‌کند. | LOG-0013 تا LOG-0015 و attestation مالک در LOG-0017؛ هیچ secret ثبت نشده است. | CLOSED |
| RISK-0003 | P0-A / Backup and restore | High | مقصد و طرح target تعیین شده‌اند (Google Drive رمزنگاری‌شده با restic/rclone)، ابزارها، OAuth/read access، repository، snapshotهای PostgreSQL/media/config، check، retention، timer روزانه و restore file-level ایزوله تأیید شده‌اند. از 2026-08-16 CMS postgres (`taha-cms-db-1`) روی production زنده است. Owner 2026-08-17 installed refreshed `taha-platform-backup`, `--dry-run` PASS, systemd job SUCCESS, restic snapshot `3afdfc96` tagged `production,cms,postgres` (`/cms-postgres-all.sql`), isolated restore into throwaway `postgres:17-alpine` as DB `taha_cms` (75 tables including `content_article`/`wagtailcore_page`; migrations content 0001–0004 + security 0001–0002), then `docker rm` + `rm -rf` restore dir. | Closed: CMS dump is in the daily restic set and isolated import evidence exists. | Project owner | Closed 2026-08-17 | Keep daily timer; restore only into disposable postgres, never live `taha-cms`. | LOG-0140; `P3-cms-backup-restore-task-spec.md`; journal `taha-platform-backup.service` 2026-08-17 10:42 UTC. | CLOSED |
| RISK-0004 | P0-A / Existing production infrastructure | High | یک Compose stack زنده با frontend، backend و PostgreSQL سالم روی VPS اجرا می‌شود. inventory کامل آن اکنون موجود است: `docker ps` در 2026-08-16 07:19 UTC سه container سالم را تأیید کرد — `taha-prod-frontend-1` روی 127.0.0.1:13000، `taha-prod-backend-1` روی 127.0.0.1:18080، `taha-prod-postgres-1`. direct-origin TLS diagnosis با بسته‌شدن `DEFER-0005` کامل شد و مکانیک deploy این پروژه stack موجود را restart یا reuse نمی‌کند (DEPLOY_RUNBOOK). | بستن: stack با inventory کامل شناخته شد؛ rollback پروژه مستقل از stack است (Caddyfile backup + atomic `current` switch)؛ staging نیز decommission شد (ADR-0025). | Project owner | Closed 2026-08-16 | پروژه هیچ container/volume موجود را restart یا reuse نمی‌کند؛ هر تغییر Caddy فقط با backup timestamped، validate و reload. | LOG-0018 تا LOG-0022؛ `docker ps` 2026-08-16 07:19 UTC؛ DEPLOY_RUNBOOK؛ ADR-0025. | CLOSED |
| RISK-0005 | P0-A / Patch posture | Medium | سیستم ۵۷ update آمادهٔ اعمال دارد، درحالی‌که unattended-upgrades فعال است؛ نوع دقیق packageها و اثر update دستی بر سرویس‌های موجود هنوز بررسی نشده است. | update کورکورانه می‌تواند Caddy/Docker یا سایت فعلی را restart کند؛ نادیده گرفتن طولانی‌مدت نیز patch risk دارد. | Project owner | پس از inventory و پیش از deploy پروژه | فهرست packageهای pending و پنجرهٔ maintenance/rollback موجود بررسی شود؛ سپس update کنترل‌شده و service smoke ثبت شود. | LOG-0018؛ MOTD و audit owner در 2026-08-14. | OPEN |
| RISK-0006 | P0-A / SSH attack surface | Medium | UFW پورت‌های 22 و 2222 را روی IPv4/IPv6 باز نشان می‌دهد؛ هر دو توسط SSH daemon listen می‌شوند و علت/نیاز پورت دوم هنوز ثبت نشده است. | key-only policy فعال است، اما public listener اضافی سطح حمله و عملیات را افزایش می‌دهد. | Project owner | پس از inventory و پیش از production change | تعیین port canonical و وابستگی clients/VPN؛ سپس حذف کنترل‌شدهٔ listener/firewall اضافی فقط با دو session سالم و rollback. | LOG-0018؛ audit owner در 2026-08-14. | OPEN |
| RISK-0007 | P0-A / Co-hosted staging capacity | High | VPS به Ubuntu 26.04 LTS با 2 vCPU / ~3910 MB RAM (~4 GiB) / 30 GB disk (~17 GB free) ارتقا یافت و مالک تصمیم گرفت plan 4 GiB را نگه دارد (2026-08-15). staging به‌طور کامل decommission شد (ADR-0025)، بنابراین co-hosted staging دیگر وجود ندارد؛ CMS runtime آینده روی همان host اجرا می‌شود. | بستن: تصمیم ظرفیت مالک گرفته شد و نیاز به staging runtime از بین رفت. | Project owner | Closed 2026-08-15 | plan 4 GiB حفظ شد؛ استقرار runtime آینده (CMS) روی همان host با review منابع انجام می‌شود؛ upgrade/گزینهٔ دیگر با ADR جدید. | تصمیم مالک 2026-08-15؛ تأیید مجدد مالک 2026-08-16 (keep the 4 GiB plan — LOG-0111)؛ ADR-0025؛ LOG-0110. | CLOSED |
| RISK-0008 | Agent tooling / 9Router credential exposure | High | یک credential برای 9Router در گفت‌وگو ارسال شد. مقدار credential در repository، ledger، commit، log یا خروجی ثبت نشده است. | هر credential ارسال‌شده در گفت‌وگو باید exposed تلقی شود؛ استفاده از آن می‌تواند دسترسی غیرمجاز ایجاد کند. | Project owner | پیش از اتصال 9Router به OpenCode یا هر agent | credential در dashboard 9Router revoke/rotate شود؛ credential تازه فقط در password manager یا flow تعاملی محلی نگه‌داری شود؛ agentها هرگز `.env` یا secret store را نخوانند. | گزارش رخداد 2026-08-14؛ هیچ مقدار credential ذخیره نشده است. | BLOCKED |

| RISK-0009 | P3 / CMS runtime deploy | High | CMS runtime live on production with Wagtail admin, `/static/*`, `/health/` split, TOTP enrollment UI, and owner-confirmed password rotate + TOTP enroll (2026-08-16). Optional residual: www site block may still lack `/static/*` if it mirrors admin. | Closed: deploy + MFA residuals completed per owner attestation. | Project owner | Closed 2026-08-16 | DEFER-0015 CLOSED (recovery codes in repo — owner rebuild); do not expose `/api/`/`/media/` without a Task Spec. | Owner VPS rebuild to `95a740f`, smoke PASS; owner attestation password+TOTP done. | CLOSED |

| RISK-0010 | ADM / Admin cutover + content preservation | High | SPA cutover is done. Production CMS is on `b6bea6a` with `content.0008` / `composition.0002` applied (2026-08-19). Remaining: later Wagtail image/RichText replacement and MFA lockout if Wagtail is uninstalled too early. Keep dumpdata + backup before **future** migrates. | Phased and reversible. | Project owner | ADM-0 remainder | Branch from `origin/main`; dumpdata before the next schema change; rollback = previous CMS image (`9940e79` before `b6bea6a`). | ADR-0026; owner migrate log 2026-08-19. | OPEN |
| RISK-0012 | CD / GitHub-driven production migrate | High | Automating `update-cms.sh` from Actions can migrate production Postgres on every `main` push. A bad image or failed backup would still restart CMS. | Slice 2 of ADR-0027; first run must be owner-attended. | Project owner | After Slice 1 `web` image | Backup + sha pin + smoke + rollback to previous image; do not enable unattended migrate in Slice 0/1. | ADR-0027; cms-origin-and-full-stack-cd-task-spec. | OPEN |
| RISK-0011 | ADM / Long-running rebuild abandonment | Medium | A 2–4 month phased custom-admin rebuild carries the same abandonment risk the owner's two previous custom-admin projects had (both live in `Samples/`). | Phases are large; without independent value per phase the work can stall. | Project owner | Throughout ADM-0..ADM-6 | Each phase is independently usable; SPA is already the live admin; Wagtail stays as `/admin-wagtail/` fallback; per-phase Task Specs. | ADR-0026; Task-list §17. | OPEN |
