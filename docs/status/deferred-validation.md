# Deferred Validation and Skipped Work

> فقط کاری را ثبت کنید که آگاهانه skip/defer شده است. این فایل به معنی تأیید یا تکمیل نیست؛ برای سیاست کامل به `docs/governance/DOCUMENTATION_POLICY.md` مراجعه کنید.

| ID | Phase/Slice | Severity | Description | Why deferred | Owner | Target phase/date | Mitigation/fallback | Evidence/command | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEFER-0001 | P0-G0 / Inventory | Low | فهرست کامل Python launcher با `py -0p` ثبت نشد. | فرمان inventory تجمیعی timeout شد؛ candidateهای موردنیاز جداگانه تأیید شدند. | Project owner | پیش از freeze `PROJECT_MANIFEST.md` | تا انتخاب نسخهٔ Python، هیچ `.venv` یا dependency پروژه‌ای ساخته نمی‌شود. | inventory session 2026-08-14؛ Python 3.14.4 و Hermes Python 3.11.15 جداگانه بررسی شدند. | OPEN |
| DEFER-0002 | P0-G0 / Tooling | Low | اتصال Context7/MCP راه‌اندازی نشده است. | طبق گزارش مالک، OAuth/endpoint با Security Checkpoint یا 403 مسدود شد؛ blocker توسعه نیست. | Project owner | پس از P0-G0، پیش از نیاز واقعی به Context7 | اتصال manual با API key و نسخهٔ واقعی OpenCode؛ تا آن زمان از منابع رسمی و محلی استفاده شود. | گزارش ارائه‌شده توسط مالک در 2026-08-14. | OPEN |
| DEFER-0003 | P0-G0 / Python environment | Low | محیط پروژه‌ای `.venv` ساخته نشده و Python 3.12 هنوز روی control plane نصب نشده است. | baseline اکنون Python 3.12 است، اما bootstrap CMS و `pyproject.toml` هنوز مجاز نیستند؛ Python پیش‌فرض متعلق به Hermes است. | Project owner | هنگام bootstrap مجاز CMS، پس از Manifest/ADR | نصب آخرین patch پشتیبانی‌شدهٔ Python 3.12 و ساخت `.venv` مستقل با `uv`؛ استفاده از Hermes ممنوع است. | `where.exe python`، version inventory LOG-0004 و Python decision LOG-0007. | OPEN |
| DEFER-0004 | P0-G0 / CI platform | Low | Gitea و self-hosted runner پیاده‌سازی نشده‌اند. | تصمیم CI گرفته شد: GitHub Actions hosted برای repository عمومی رایگان است و VPS برای runner/Gitea ظرفیت کافی ندارد. | Project owner | Closed 2026-08-14 | GitHub Actions hosted در ADR-0009؛ بازنگری فقط با ADR و evidence جدید. | LOG-0006 و LOG-0007؛ ADR-0009. | CLOSED |
| DEFER-0005 | P0-A / Staging direct-origin TLS | Medium | پس از Caddy placeholder، نخستین `curl --resolve` مستقیم به origin یک TLS internal alert داد. | Caddy log نشان داد probe قبل از تکمیل ACME certificate issuance اجرا شده بود؛ re-test post-issuance با HTTP/2 503 موفق شد. | Project owner | Closed 2026-08-14 | Cloudflare zone در Full باقی می‌ماند؛ Full(strict) همچنان به تصمیم/verification جداگانه نیاز دارد. | LOG-0022 تا LOG-0024؛ Caddy certificate evidence و re-test 2026-08-14. | CLOSED |
| DEFER-0006 | P0-A / Temporary restore target inventory | Low | یک directory قدیمیِ `deploy`-owned و mode `0700` زیر `/dev/shm` پیش از rehearsal وجود داشت و برای safety نه reuse و نه حذف شد. | ownership/content آن خارج از scope rehearsal بود؛ target یکتای جدید با `mktemp` ایجاد شد و cleanup موفق داشت. | Project owner | پیش از پاک‌سازی tmpfs یا maintenance بعدی | فقط non-sensitive inventory انجام شود؛ حذف تنها پس از تأیید مالک path و عدم نیاز به محتوا. | LOG-0039 تا LOG-0040؛ `stat` owner/mode/type. | OPEN |
| DEFER-0007 | P1 / Contact path | Low | مقصد تماس امن و تأییدشده (ایمیل/profile) هنوز تعیین نشده؛ P1 footer حالت صادقانهٔ «not published» را نشان می‌دهد و هیچ فرم/backend/analytics تماس وجود ندارد. | هیچ مقصد تأییدشده‌ای برای انتشار وجود ندارد؛ ساخت ایمیل/لینک حدسی ممنوع است. | Project owner | Closed 2026-08-15 | تصمیم مالک در فرم P2-C1: تماس در P2 منتشر نمی‌شود (گزینهٔ c)؛ حالت صادقانهٔ «not published» حفظ می‌شود؛ هیچ فرم/backend/analytics وجود ندارد. | P2-C1-CONTENT-REQUEST؛ LOG-0085. | CLOSED |
| DEFER-0008 | P1 / Self-hosted font | Low | خانوادهٔ فونت دوزبانه انتخاب و self-host شد: `Vazirmatn Variable` برای فارسی و `Inter Variable` برای لاتین. | تصمیم specimen/مجوز/ساخت با ADR-0019 و build/audit واقعی تکمیل شد. | Project owner | Closed 2026-08-15 | جایگزینی آینده فقط با specimen و ADR جدید. | ADR-0019؛ `npm run check`/`build`/`npm audit`؛ LOG-0062. | CLOSED |
| DEFER-0009 | P1 / OG image | Low | تصویر OG/social preview وجود ندارد؛ meta OG بدون image منتشر می‌شود. | هیچ asset تأییدشده/مالکیت‌دار موجود نیست. | Project owner | پیش از production release | انتشار بدون og:image؛ اضافه‌شدن فقط با asset تأییدشده و alt/context. | LOG-0054. | OPEN |
| DEFER-0010 | P1 / Browser-level verification | Medium | smoke مرورگر (viewport 320–1440، keyboard، skip-link، contrast، reduced-motion، RTL mixed sample) هنوز اجرا نشده است؛ evidence فعلی `astro check` و build و بررسی static خروجی است. | environment فعلی مرورگر/بررسی بصری خودکار ندارد؛ پیش از release باید در CI یا محیط مالک اجرا شود. | Project owner | Closed 2026-08-15 | بررسی دستی/خودکار مرورگر پیش از deploy؛ main content بدون JS خوانا باقی می‌ماند. | `npm run check`/`build` PASS در LOG-0054. | CLOSED |
| DEFER-0011 | P1 / Cloudflare robots behavior | Medium | Cloudflare edge برای `staging.tahamohamadi.ir/robots.txt` متن «content signals» برمی‌گرداند، درحالی‌که direct-origin robots artifact صحیح را می‌دهد. | رفتار در edge/zone خارج از Caddy و repository است و باید در Cloudflare rule/worker/settings بررسی شود. | Project owner | پیش از production release | direct-origin artifact robots صحیح است؛ تا رفع، staging noindex با header `X-Robots-Tag` محافظت می‌شود و production deploy robots بدون zone review انجام نمی‌شود. | direct-origin vs proxied curl، LOG-0060. | OPEN |
| DEFER-0012 | P1-T01 / external design resources | Low | **Beautiful UI** یک وب‌سایت copy-paste با مجوز **MIT** است (نه پکیج npm): منبع/مجوز تأیید شده است، اما adoption آن به یک interaction slice مجزا با Task Spec و QA (no-JS/reduced-motion/RTL/bundle) وابسته است. **UI8 DNA** محصول تجاریِ مارکت‌پلیس است و بدون خرید/لایسنسِ مالک فایلِ نسخه‌دار و حق استفاده در دسترس نیست. | Beautiful UI نصب‌کردنی نیست و UI8 دسترسی/لایسنس نمی‌دهد؛ کپی بدون تأیید source+use-right ممنوع است. | Project owner | پیش از adoption هر component (Beautiful UI) یا تهیهٔ UI8 | استفاده از Beautiful UI فقط در slice مصوب با ثبت source+MIT و خروجی منحصراً از tokenهای خود پروژه؛ UI8 فقط پس از خرید و ثبت license/version توسط مالک. | BeautifulUI (MIT) و UI8 (paid) reviewed 2026-08-15؛ LOG-0067/LOG-0073. | OPEN (split) |
| DEFER-0013 | P1/P2 / Mobile-browser visual matrix + 200% zoom | Low | اجرای محلی و hosted CI ماتریس overflow و About-tabs را پوشش می‌دهد؛ بررسی visual کامل touch/stacking/typography و شواهد zoom واقعی 200٪ مرورگر در عرض‌های 320/390/768/1024/1280/1440 همچنان باقی است. | zoom واقعی 200٪ را نمی‌توان با viewport مصنوعی به‌صورت وفادار شبیه‌سازی کرد (fake zoom ممنوع است)؛ environment فعلی اجرای visual واقعیِ zoom را ندارد. | Project owner | پیش از release بعدی یا maintenance | CI job‌های `About tabs regression (Playwright)` با شش عرض (`apps/web/qa/about-tabs.spec.mjs`) و `Mobile overflow check (Playwright)` (`apps/web/qa/mobile-overflow.spec.mjs`)؛ بررسی visual تکمیلی و zoom واقعی 200٪ به‌صورت دستی توسط مالک روی 320/390/768/1024/1280/1440. | اجرای محلی LOG-0096: 78 PASS با Chromium واقعی (1228 headless shell از طریق `PW_EXECUTABLE_PATH`); run `31903433836` با type check، build، smoke، هر دو Playwright regression، audit و artifact checks همگی PASS؛ run `31904100378` نیز PASS؛ مسیر صحیح specs: `apps/web/qa/…` (پیش‌تر به‌اشتباه `infra/qa/…` ثبت شده بود). | OPEN |
