# Deferred Validation and Skipped Work

> فقط کاری را ثبت کنید که آگاهانه skip/defer شده است. این فایل به معنی تأیید یا تکمیل نیست؛ برای سیاست کامل به `docs/governance/DOCUMENTATION_POLICY.md` مراجعه کنید.

| ID | Phase/Slice | Severity | Description | Why deferred | Owner | Target phase/date | Mitigation/fallback | Evidence/command | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEFER-0001 | P0-G0 / Inventory | Low | فهرست کامل Python launcher با `py -0p` ثبت نشد. | فرمان inventory تجمیعی timeout شد؛ candidateهای موردنیاز جداگانه تأیید شدند. | Project owner | پیش از freeze `PROJECT_MANIFEST.md` | تا انتخاب نسخهٔ Python، هیچ `.venv` یا dependency پروژه‌ای ساخته نمی‌شود. | inventory session 2026-08-14؛ Python 3.14.4 و Hermes Python 3.11.15 جداگانه بررسی شدند. | OPEN |
| DEFER-0002 | P0-G0 / Tooling | Low | اتصال Context7/MCP راه‌اندازی نشده است. | طبق گزارش مالک، OAuth/endpoint با Security Checkpoint یا 403 مسدود شد؛ blocker توسعه نیست. | Project owner | پس از P0-G0، پیش از نیاز واقعی به Context7 | اتصال manual با API key و نسخهٔ واقعی OpenCode؛ تا آن زمان از منابع رسمی و محلی استفاده شود. | گزارش ارائه‌شده توسط مالک در 2026-08-14. | OPEN |
| DEFER-0003 | P0-G0 / Python environment | Low | محیط پروژه‌ای `.venv` ساخته نشده و Python 3.12 هنوز روی control plane نصب نشده است. | baseline اکنون Python 3.12 است، اما bootstrap CMS و `pyproject.toml` هنوز مجاز نیستند؛ Python پیش‌فرض متعلق به Hermes است. | Project owner | Closed 2026-08-15 | آخرین patch پشتیبانی‌شدهٔ Python 3.12 (3.12.13) با `uv` نصب شد و `.venv` پروژه‌محلی در `apps/cms/` با `uv sync` ساخته شد؛ Hermes استفاده نشد. | `uv python install 3.12` → 3.12.13؛ `uv sync --python 3.12` (55 پکیج، uv.lock)؛ 62 pytest PASS در LOG-0107. | CLOSED |
| DEFER-0004 | P0-G0 / CI platform | Low | Gitea و self-hosted runner پیاده‌سازی نشده‌اند. | تصمیم CI گرفته شد: GitHub Actions hosted برای repository عمومی رایگان است و VPS برای runner/Gitea ظرفیت کافی ندارد. | Project owner | Closed 2026-08-14 | GitHub Actions hosted در ADR-0009؛ بازنگری فقط با ADR و evidence جدید. | LOG-0006 و LOG-0007؛ ADR-0009. | CLOSED |
| DEFER-0005 | P0-A / Staging direct-origin TLS | Medium | پس از Caddy placeholder، نخستین `curl --resolve` مستقیم به origin یک TLS internal alert داد. | Caddy log نشان داد probe قبل از تکمیل ACME certificate issuance اجرا شده بود؛ re-test post-issuance با HTTP/2 503 موفق شد. | Project owner | Closed 2026-08-14 | Cloudflare zone در Full باقی می‌ماند؛ Full(strict) همچنان به تصمیم/verification جداگانه نیاز دارد. | LOG-0022 تا LOG-0024؛ Caddy certificate evidence و re-test 2026-08-14. | CLOSED |
| DEFER-0006 | P0-A / Temporary restore target inventory | Low | یک directory قدیمیِ `deploy`-owned و mode `0700` زیر `/dev/shm` پیش از rehearsal وجود داشت و برای safety نه reuse و نه حذف شد. | ownership/content آن خارج از scope rehearsal بود؛ target یکتای جدید با `mktemp` ایجاد شد و cleanup موفق داشت. | Project owner | پیش از پاک‌سازی tmpfs یا maintenance بعدی | فقط non-sensitive inventory انجام شود؛ حذف تنها پس از تأیید مالک path و عدم نیاز به محتوا. | LOG-0039 تا LOG-0040؛ `stat` owner/mode/type. | OPEN |
| DEFER-0007 | P1 / Contact path | Low | مقصد تماس امن و تأییدشده (ایمیل/profile) هنوز تعیین نشده؛ P1 footer حالت صادقانهٔ «not published» را نشان می‌دهد و هیچ فرم/backend/analytics تماس وجود ندارد. | هیچ مقصد تأییدشده‌ای برای انتشار وجود ندارد؛ ساخت ایمیل/لینک حدسی ممنوع است. | Project owner | Closed 2026-08-15 | تصمیم مالک در فرم P2-C1: تماس در P2 منتشر نمی‌شود (گزینهٔ c)؛ حالت صادقانهٔ «not published» حفظ می‌شود؛ هیچ فرم/backend/analytics وجود ندارد. | P2-C1-CONTENT-REQUEST؛ LOG-0085. | CLOSED |
| DEFER-0008 | P1 / Self-hosted font | Low | خانوادهٔ فونت دوزبانه انتخاب و self-host شد: `Vazirmatn Variable` برای فارسی و `Inter Variable` برای لاتین. | تصمیم specimen/مجوز/ساخت با ADR-0019 و build/audit واقعی تکمیل شد. | Project owner | Closed 2026-08-15 | جایگزینی آینده فقط با specimen و ADR جدید. | ADR-0019؛ `npm run check`/`build`/`npm audit`؛ LOG-0062. | CLOSED |
| DEFER-0009 | P1 / OG image | Low | تصویر OG/social preview وجود ندارد؛ meta OG بدون image منتشر می‌شود. | هیچ asset تأییدشده/مالکیت‌دار موجود نیست. Owner decision 2026-08-16: the Gemini-generated images in `Assets/` are 2048x2048 contact sheets (3x3 mascot grids) with white backgrounds, AI artifacts and visible Apple logos — NOT production-ready as og:image; DEFER-0009 stays OPEN until a real image is provided. | Project owner | پیش از production release (OPEN — awaiting a real image) | انتشار بدون og:image؛ اضافه‌شدن فقط با asset تأییدشده و alt/context. | LOG-0054؛ owner decision 2026-08-16؛ visual-review report of `Assets/` contact sheets. | OPEN |
| DEFER-0010 | P1 / Browser-level verification | Medium | smoke مرورگر (viewport 320–1440، keyboard، skip-link، contrast، reduced-motion، RTL mixed sample) هنوز اجرا نشده است؛ evidence فعلی `astro check` و build و بررسی static خروجی است. | environment فعلی مرورگر/بررسی بصری خودکار ندارد؛ پیش از release باید در CI یا محیط مالک اجرا شود. | Project owner | Closed 2026-08-15 | بررسی دستی/خودکار مرورگر پیش از deploy؛ main content بدون JS خوانا باقی می‌ماند. | `npm run check`/`build` PASS در LOG-0054. | CLOSED |
| DEFER-0011 | P1 / Cloudflare robots behavior | Medium | رفتار Cloudflare edge برای `staging.tahamohamadi.ir/robots.txt` دیگر قابل بررسی نیست: staging به‌طور کامل decommission شد (ADR-0025، 2026-08-15) — Caddy block از VPS حذف و DNS record در صورت وجود حذف شد. | بستن: hostname staging دیگر وجود ندارد؛ production robots/sitemap در هر deploy review می‌شود. | Project owner | Closed 2026-08-15 | دیگر هیچ hostname staging با رفتار edge متفاوت وجود ندارد؛ production robots در deploy review می‌شود (DEPLOY_RUNBOOK). | ADR-0025؛ LOG-0110؛ direct-origin vs proxied curl، LOG-0060. | CLOSED |
| DEFER-0012 | P1-T01 / external design resources | Low | **Beautiful UI** یک وب‌سایت copy-paste با مجوز **MIT** است (نه پکیج npm): منبع/مجوز تأیید شده است، اما adoption آن به یک interaction slice مجزا با Task Spec و QA (no-JS/reduced-motion/RTL/bundle) وابسته است. **UI8 DNA** محصول تجاریِ مارکت‌پلیس است و بدون خرید/لایسنسِ مالک فایلِ نسخه‌دار و حق استفاده در دسترس نیست. | Beautiful UI نصب‌کردنی نیست و UI8 دسترسی/لایسنس نمی‌دهد؛ کپی بدون تأیید source+use-right ممنوع است. | Project owner | پیش از adoption هر component (Beautiful UI) یا تهیهٔ UI8 | استفاده از Beautiful UI فقط در slice مصوب با ثبت source+MIT و خروجی منحصراً از tokenهای خود پروژه؛ UI8 فقط پس از خرید و ثبت license/version توسط مالک. | BeautifulUI (MIT) و UI8 (paid) reviewed 2026-08-15؛ LOG-0067/LOG-0073. | OPEN (split) |
| DEFER-0013 | P1/P2 / Mobile-browser visual matrix + 200% zoom | Low | CI job‌های `About tabs regression (Playwright)` (شش عرض 320/390/768/1024/1280/1440 × هر دو About locale: overflow، centering، cards-wider، tabs) و `Mobile overflow check (Playwright)` اکنون در hosted CI سبزند؛ بررسی visual کامل touch/stacking/typography و شواهد zoom واقعی 200٪ مرورگر در همان عرض‌ها همچنان باقی است. | zoom واقعی 200٪ را نمی‌توان با viewport مصنوعی به‌صورت وفادار شبیه‌سازی کرد (fake zoom ممنوع است)؛ environment فعلی اجرای بصری مرورگر واقعی برای zoom ندارد. | Project owner | پیش از release بعدی یا maintenance | CI job‌های `About tabs regression (Playwright)` با شش عرض (`apps/web/qa/about-tabs.spec.mjs`) و `Mobile overflow check (Playwright)` (`apps/web/qa/mobile-overflow.spec.mjs`)؛ بررسی visual تکمیلی و zoom واقعی 200٪ به‌صورت دستی توسط مالک روی 320/390/768/1024/1280/1440. | اجرای محلی LOG-0096: 78 PASS با Chromium واقعی (1228 headless shell از طریق `PW_EXECUTABLE_PATH`)؛ hosted CI run `31903433836` (commit `d69b0e9`) برای Mobile overflow و About tabs regression (شش عرض) PASS و run `31904100378` (commit `e1fcac0`) نیز PASS؛ مسیر صحیح specs: `apps/web/qa/…` (پیش‌تر به‌اشتباه `infra/qa/…` ثبت شده بود). | OPEN |
| DEFER-0014 | P3-05 / Media alt-by-locale | Low | Alt-by-locale media fields (`alt_text_fa` / `alt_text_en`) shipped in ADM-2 with additive migration `0002`. | Closed 2026-08-18: bilingual alt is on the Media model and admin SPA. | Project owner | Closed 2026-08-18 | Use `alt_text_fa`/`alt_text_en` in the media library; `alt_text` remains the fallback single field. | LOG-0159; Task-list ADM-2. | CLOSED |
| DEFER-0015 | P3-06 / TOTP recovery codes | Low | Authenticator-app TOTP enrollment and login are implemented; backup/recovery codes and device disable/re-enroll UX are not. | Minimal enroll+login unblocks RISK-0009; recovery codes need a separate security Task Spec (storage, one-time use, audit). | Project owner | Closed 2026-08-16 | Hashed recovery codes (64-bit, reveal-once), login fallback, regenerate/disable under `/admin/account/two-factor/`; audit `mfa.recovery_*` / `mfa.disabled`. Owner rebuild to pick up on production. | LOG-0131; P3-mfa-totp-recovery-codes-task-spec; ADR-0020. | CLOSED |
| DEFER-0016 | P3-07 / Public preview token | Low | Staff-only preview under `/admin-wagtail/preview/` is implemented (session + MFA). Signed/public share-token preview is not. | Staging removed (ADR-0025); public tokens need replay/expiry/leak design beyond the P3-07 safe minimum. | Project owner | Separate Task Spec when external draft review is required | Staff preview remains the access boundary; do not publish draft via `/api/` or unauthenticated URLs. | LOG-0132; P3-preview-boundary-task-spec; ADR-0022. | OPEN |
| DEFER-0017 | P4–P6 / Public Caddy `/api/` (blog + research + projects) | Medium | In-process Ninja APIs ship for blog (P4), research (P5), and projects/case studies (P6); public edge proxy for `/api/` (and `/media/`) is not opened. | Closed 2026-08-17: owner merged `/api/*` + `/media/*` into production Caddy; public `https://tahamohamadi.ir/api/research/topics/en` and `/api/articles/en` return 200 JSON. | Project owner | Closed 2026-08-17 | Public read-only Ninja projection; drafts still unpublished. | Caddy `handle /api/*` line 65; LOG-0143; smoke 200. | CLOSED |
| DEFER-0018 | P4 / RSS-Atom feed | Low | Optional `/{locale}/blog/feed.xml` RSS/Atom is not implemented in this P4 slice. | Prefer shipping SEO + redirects first; feed needs dedicated tests and `rel=alternate` wiring. | Project owner | P4 follow-up or owner decision | Blog routes + sitemap + BlogPosting JSON-LD ship without feed links. | P4-blog-writing-task-spec; plan S5. | OPEN |
| DEFER-0019 | P5 / Research Statement PDF | Low | ResearchStatement ships as rich-text body only; downloadable/tailored PDF is not implemented. | Public `/media/` delivery is unpublished; PDF needs media contract + owner assets. | Project owner | After publish-media Task Spec | Statement HTML route only; no fabricated PDF links. | P5-research-task-spec. | OPEN |
| DEFER-0020 | P5 / Curated collections + complex research graph | Low | Topic→Project→Publication is list/tree navigation only; curated collections and D3/complex graph viz are out of P5. | Scope control; accessibility fallback is the product requirement for P5. | Project owner | Post-P5 / P10 | Accessible relationship lists only. | P5-research-task-spec; master plan P5. | OPEN |
| DEFER-0021 | P6 / Live demo embed / iframe | Low | Case study pages link to external demo URLs when public; embedded iframe/live demo player is not implemented. | Out of P6 scope; needs security review for third-party embeds and CSP. | Project owner | Post-P6 Task Spec | External links only with `rel=noopener noreferrer`; no iframe without separate Spec. | P6-case-studies-task-spec. | OPEN |
| DEFER-0022 | P8 / Local HTTP preview bind | Medium | Local Playwright/preview sign-off for the new About detail routes was not completed in this Windows environment. Dist-only `qa/cms-profile-build.spec.mjs` and hosted CI cover the built artifact. | Local `astro preview` bind failed with `listen EACCES` on loopback ports in the prior integration session. | Project owner | Next local verification session or hosted CI on this PR | Offline snapshot when `CMS_API_BASE` unset + CMS-profile build spec (incl. fail-build on outage, LOG-0182) + web CI. | LOG-0150; LOG-0182. | OPEN |
| DEFER-0023 | ADM / Wagtail→custom admin transition | Low | ADM-1 cutover shipped: SPA serves `/admin/`; Wagtail moved to `/admin-wagtail/` for TOTP HTML, preview, and rollback. | Closed 2026-08-18: cutover is in production image path (LOG-0163). Remaining uninstall is `DEBT-0003`. | Project owner | Closed 2026-08-18 | Keep previous CMS image for rollback; TOTP enrollment HTML stays on `/admin-wagtail/` until SPA MFA is proven. | LOG-0163; ADR-0026 Cutover. | CLOSED |
| DEFER-0024 | ADM / Branch base | Low | Custom-admin work now branches from current `origin/main` (cutover + P4–P6 models live there). | Closed for new work: `feat/continue-admin-public-sync` starts from `origin/main`. | Project owner | Closed 2026-08-19 | Keep branching ADM slices from `origin/main`. | git checkout 2026-08-19. | CLOSED |
| DEFER-0025 | Web / Dark mode | Low | Full dark mode is not implemented; the site is light-only. | No approved interaction slice; would need tokens, contrast re-audit, RTL/LTR and no-JS verification. | Project owner | بعدی (فاز جدا با Task Spec) | Light theme is the only theme; no partial dark CSS. | PROJECT_MANIFEST P1 decisions table; §14 U5 (custom-admin-rebuild-fa.md). | OPEN |
| DEFER-0026 | ADM-6 / Playwright lifecycle suite | Low | Full Playwright config (retries/trace/video/html) and browser create→publish→public fa/en UI flow are not in this slice. | Pytest covers the JSON lifecycle; browser matrix needs hosted Playwright config beyond existing QA specs. | Project owner | ADM-6 follow-up | `apps/web/qa/` node specs + CMS pytest lifecycle remain the automated evidence. | ADM-6-frontend-wiring-task-spec.md. | OPEN |
| DEFER-0027 | ADM-6 / Production HMAC rebuild enable | Medium | `REBUILD_TRIGGER_ENABLED` stays False. Code path now targets `infra/deploy/rebuild-web.sh` (not `rebuild-static.sh`). Production must not enable HMAC until the owner smoke-tests loopback `rebuild-web.sh` and confirms CMS can invoke it. | Enabling without a working script path would report success or hang the CMS. | Project owner | After VPS `rebuild-web.sh` smoke + HMAC enable rehearsal | Keep CD / manual `rebuild-web.sh` as the production rebuild path until enablement. | `apps/cms/apps/rebuild/services.py`; `infra/deploy/rebuild-web.sh`; LOG-0183. | OPEN |
| DEFER-0028 | ADM-6 / Composition + tokens + CV projection | Low | Composition pages, `primaryColor` CSS injection, and admin-managed current CV were not wired into Astro in the ADM-6 listing slice. | Closed 2026-08-19 for **blog story→Astro**: published story JSON + `StoryBody.astro` + article RichText fallback. Remaining CV/`primaryColor` is `DEFER-0029`; later entity stories are `DEFER-0030`. | Project owner | Closed 2026-08-19 | Blog articles use story when published; otherwise sanitized `article.body`. | LOG-0167; blog-story-composition-task-spec. | CLOSED |
| DEFER-0029 | ADM-6 / primaryColor + admin CV | Low | `primaryColor` CSS injection and an admin-managed current CV document are not wired into Astro. | Split from `DEFER-0028` after the blog story slice. | Project owner | ADM-6 follow-up | Public site keeps existing tokens; CV markdown downloads unchanged (`DEBT-0006`). | Task-list §17 ADM-6; DEBT-0006. | OPEN |
| DEFER-0030 | Story composition / later types | Low | Project, research, and experience story bodies are not attached to composition in this slice. | Blog is the reference implementation first. | Project owner | After blog story is live | Those pages keep current field dumps / existing projections. | blog-story-composition-task-spec. | OPEN |
| DEFER-0031 | ADR-0027 / Caddy in Compose | Medium | TLS edge stays host Caddy until `web` nginx is proven on loopback. Moving ACME into Compose is a separate cutover. | Avoid dual TLS change in the first web-image slice. | Project owner | After Slice 1 | Host Caddyfile backup + validate + reload; keep previous `web` image. | ADR-0027 Slice 4. | OPEN |
