# Technical Debt

> فقط debt غیرحاد را ثبت کنید. ریسک release، security یا blocker در `RISK_REGISTER.md` و آزمون deferشده در `deferred-validation.md` ثبت می‌شود.

| ID | Phase/Slice | Severity | Description | Why deferred | Owner | Target phase/date | Mitigation/fallback | Evidence/command | Status |
|---|---|---|---|---|---|---|---|---|---|
| DEBT-0001 | P1 / Design baseline alignment | Low | `docs/design.md` still documents React/shadcn/Radix/GSAP/D3/Three as the target frontend architecture while the P1 build is intentionally static-only with none of those installed. | This is a vision-vs-implementation phase gap, not a defect; P1 is a scoped subset. | Project owner | پیش از P3 (زمان معرفی islands/CMS) | ADR-0016/0018 scoped P1 static-only; P3 slices must explicitly decide which vision components enter with tested Task Specs, and must not silently re-introduce the blocked stack. | verification report LOG-0055؛ ADR-0016/0018. | OPEN |
| DEBT-0002 | P2 / About evidence scan | Medium | Tab mode hides inactive panels with `display: none`; find-in-page needs tab selection or **Show all sections**. Per-section filters are progressive enhancement (small inline script). | P2-H stacked layout caused excessive vertical scroll; owner prefers compact tabs with optional full scan. | Project owner | About UX follow-up if show-all insufficient | Sticky tab toolbar + show-all toggle + per-section search/chip filters. | LOG-0149; Playwright about-tabs PASS. | OPEN (mitigated) |
| DEBT-0003 | ADM-0 / Wagtail remaining surface | Medium | **PARTIAL (LOG-0188):** runtime RichTextField removed (`content.0012` → TextField); local `html_sanitize` replaces `wagtail.whitelist`; content snippet/ModelViewSets unregistered (SPA-only CRUD); content image FKs already on `media.Media` (LOG-0187). Still installed: Wagtail `INSTALLED_APPS` + deps, `/admin-wagtail/` (LOGIN_URL, staff preview, profile HTML, TOTP HTML rollback), historical migrations importing `wagtail.fields`/`wagtailimages`, `require_admin_access` / hooks. | Full uninstall still needs Django login/preview cutover + migration squash or stub apps before dropping `wagtail` from `pyproject.toml`. SPA TOTP (`/admin/security` + `/api/v1/admin/auth/mfa/*`) is the primary enrollment path; Wagtail HTML MFA is rollback-only for one more cycle. | Project owner | After LOGIN_URL + staff preview no longer need Wagtail + owner dumpdata | Keep `/admin-wagtail/` until MFA proven on deployed image; never enable `CMS_CD_AUTO_MIGRATE`. | ADR-0026; LOG-0163; LOG-0187; LOG-0188; grep wagtail in `apps/cms`. | PARTIAL |
| DEBT-0004 | ADM-2 / MediaPicker → content editors | Low | The reusable `MediaPicker` is wired into the composition editor (ADM-3) for media/mediaList blocks, and into article featured-image + project diagram/screenshot assign (LOG-0187). | Resolved for composition + content image FKs; orphan/usage registry lists those FKs in `MEDIA_REFERENCE_FIELDS`. | Project owner | ADM-6 | Composition blocks and featured/diagram/screenshot use Media library. | Task-list §17 ADM-2/ADM-3; LOG-0159/0160/0187; admin_media.py. | CLOSED |
| DEBT-0005 | ADM-4 / Revisions + scheduled publish | Low | Revisions snapshots (immutable, restore-as-draft) and scheduled publishing (scheduled_for + management command + timer) are implemented: `ContentRevision`, `scheduled` lifecycle + `scheduled_for`, `publish_scheduled_content`, systemd unit/timer under `infra/cms/`, admin API + SPA wiring. | Was deferred for scope after ADM-4 transitions/health shipped. | Project owner | ADM-4 follow-up | Transitions include scheduled; timer is owner-installed (no Celery; no auto-migrate). | Task-list §17 ADM-4; LOG-0161; LOG-0181; `test_admin_revisions_schedule.py`. | CLOSED (2026-08-20) |
| DEBT-0006 | ADM-5 / Contact inbox + CV current-document | Low | A contact inbox and an admin-managed current CV document are not implemented. | Contact persistence stays unpublished (DEFER-0007 owner decision — no public contact form source); CV downloads are static markdown served by the Astro site and wiring them to the admin belongs to ADM-6 frontend integration. | Project owner | ADM-6 (or when contact reopens) | Contact inbox can be added when a public contact path is approved; CV document can use the media library (one-current-document policy, §14 F5) during ADM-6. | Task-list §17 ADM-5; LOG-0162. | OPEN |
