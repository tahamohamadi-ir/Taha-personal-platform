# reDesign_plan.md — بازطراحی کامل ظاهر و تجربهٔ کاربری سایت

> وضعیت: پیش‌نویس برای اجرا — این سند **جانشین همهٔ اسناد بصری قبلی** است (DESIGN-CONTRACT و امثال آن برای این بازطراحی مبنای نیستند).
> قلمرو: فقط `apps/web/` (فرانتاند عمومی). به CMS، مسیرها، دیتا و سئو دست نمی‌زنیم.
> تاریخ: 2026-08-24

---

## ۰. خلاصهٔ یک‌نگاهی

سایت امروز «بدتر از افتضاح» است چون هیچ سیستم واحدی پشتش نیست: حداقل ۵ سیستم دکمهٔ متفاوت، ۶ سبک کارت متفاوت، ۳ الگوی empty-state، ۲ تقویم تاریخ روی یک زبان، برچسب‌های انگلیسی روی صفحات فارسی، فونت‌های self-host که **هرگز لود نشده‌اند**، و یک صفحهٔ ورود زبان که با بقیهٔ سایت هیچ ربط بصری ندارد. گراف روابط پژوهش هم بی‌معنا و زشت است و حذف می‌شود.

راه‌حل: یک **سیستم طراحی مینیمالِ محتوامحور (Editorial Minimal)** با یک رنگ برند، یک جفت فونت فارسی/انگلیسی، یک زبان کامپوننت، و بازنویسی صفحه‌به‌صفحه روی آن سیستم.

---

## ۱. اصول طراحی (قابل استناد در همهٔ تصمیم‌ها)

1. **محتوا قهرمان است.** هیچ المان تزئینی حق ندارد بیش از متن جلب توجه کند. اگر یک عنصر «قشنگ» است ولی به خواندن کمک نمی‌کند، حذف.
2. **یک سیستم، همه‌جا.** هر دکمه/کارت/برچسب/حالت خالی فقط از کامپوننت‌های مشترک می‌آید. استایل inline و بلوک‌های `<style>` صفحه‌ای ممنوع، جز مورد‌های مستندشده.
3. **بدون جاوااسکریپت هم کامل.** صفحات عمومی بدون JS باید ۱۰۰٪ خوانا و قابل‌ناوبری بمانند (محدودیت معماری موجود). حرکت‌ها فقط CSS و ظریف.
4. **دوجهانگی واقعی.** fa و en از نظر کیفیت تایپوگرافی هم‌ترازند؛ RTL با logical properties؛ تاریخ fa = جلالی با ارقام فارسی، en = میلادی.
5. **فضای سفید بی‌ترس.** تراکم کم، یک ستون محتوا با عرض ثابت، سلسله‌مراتب با فاصله و وزن، نه با خط و کادر اضافه.
6. **صادق بودن در حالت خالی.** هر بخش خالی یک پیام روشن + لینک اقدام دارد؛ هیچ صفحه‌ای «خالیِ سفید» نیست.
7. **یک لهجهٔ رنگی.** پس‌زمینهٔ کاغذی خنثی، جوهر تیره، و فقط **یک** رنگ برند (فیروزه‌ای هماهنگ با لوگو). طلایی/بنفش/سرمه‌ای به‌عنوان رنگ‌های رقابی حذف می‌شوند.

---

## ۲. تشخیص: مشکلات تأییدشدهٔ وضع موجود (با مدرک)

### ۲.۱ فونت — هویت تایپوگرافی عملاً وجود ندارد
- `global.css:61-62` استک `"Inter"` و `"Vazirmatn"` را صدا می‌زند، اما پکیج‌های `@fontsource-variable/*` فقط با نام `'Inter Variable'` و `'Vazirmatn Variable'` ثبت می‌شوند ⇒ **هیچ‌کدام لود نمی‌شوند** و کل سایت با Segoe UI/system-ui رندر می‌شود.

### ۲.2 صفحهٔ ورود زبان (gateway) جزیرهٔ جدا از سایت
- `pages/index.astro` شل HTML خودش را دارد: بدون Header/Footer، `theme-color: #071225` (سرمه‌ای تیره) در برابر `#f7f8f7` بقیهٔ سایت، دکمه‌های ghost با فیروزه‌ای/طلایی روی سرمه‌ای — زبانی که هیچ‌جای دیگری نیست.
- صفحهٔ 404 هم ظاهر gateway را کپی کرده؛ باقی سایت روشن است.

### ۲.۳ گراف روابط پژوهش — حذف
- `ResearchGraphIsland.tsx` (۵۱۲ خط React + کتابخانهٔ `motion`) + `ResearchGraphSection.astro` + `lib/cms/research-graph.ts` (۲۹۱ خط). خوانا نیست، کنتراست ندارد، کاربردی ندارد و همان اطلاعات را زیر خودش دوباره به‌صورت متن تکرار می‌کند.
- علاوه بر آن، هیرؤ صفحهٔ اصلی دو لایهٔ تزئین تکراری دارد: SVG متحرک + آیلند WebGL با Three.js (~600KB) که فقط دکور است.

### ۲.۴ بی‌نظمی سیستمی در کامپوننت‌ها
- **دکمه‌ها (≥۵ سیستم):** `.cta--primary/--ghost` (Landing)، چیپ سرمه‌ای `.research-cta/.project-cta/.downloads-link`، دکمهٔ فرم در ContactPage + نسخهٔ مردهٔ همان در Footer، لینک‌های outline پراکنده (`footer-switch`, `social-link`, `translation-unavailable-link`, `about-next-step-link`)، دکمه‌های native بی‌استایل.
- **کارت‌ها (≥۶ سبک):** ردیف‌کارت با چیپ CTA سرمه‌ای، کارت با حاشیهٔ رنگی کنار، کارت سادهٔ خط‌دار، ردیف زیرخط‌دار (مقالات/کاتالوگ‌ها)، چیپ فیلتر About، تگ‌های زیرخط‌دار.
- **empty-state (۳ الگو):** `.empty-state` سراسری، `<p class="empty">`، `<p class="blog-empty">`.
- **عرض کانتینر آشوبی:** 1280px / 44rem / 48rem / 42rem؛ حتی `.research-page` در دو صفحه دو عرض مختلف دارد.
- **~۲۰ نسخهٔ byte-to-byte یکسان از بلوک استایل `.catalog-page`** در صفحات کاتالوگ + درخت fa/en تقریباً کپی کامل.

### ۲.۵ بی‌قاعدگی زبانی و تاریخ روی صفحات فارسی
- برچسب‌های انگلیسی روی fa: «Email / LinkedIn / ORCID / Work / Location» در `ContactPage.astro:31-67`، «DOI: / Citations: / PDF» در `publications/[slug].astro`، و badge های `project_type` خام و ترجمه‌نشده.
- **دو تقویم:** وبلاگ تاریخ جلالی با ارقام فارسی (`fa-IR`)، اما پژوهش/پروژه/دانلودها/انتشارات/سخنرانی‌ها رشتهٔ خام ISO مثل `2026-08-16`.

### ۲.۶ رنگ‌های خارج از توکن و کد مرده
- کهربایی «غیرقابل‌دسترس» در fa `#92400e/#fef3c7` و در en `#b45309/#fef3c7` — دو رنگ برای یک مفهوم.
- توکن‌های مرده (`--color-signature-soft`, `--color-research-soft`, `--radius-xl`, `--shadow-lg`, ...)، وابستگی بی‌استفادهٔ `gsap`، استایل فرمِ مرده در Footer، Tailwind v4 که فقط برای ۲ کلاس لود می‌شود.
- باگ چسبندگی: تولبار تب‌های About با `top: 3.625rem` هاردکد شده و ~۷px زیر هدرِ چسبان می‌رود، در حالی که توکن `--space-sticky-offset` برای همین ساخته شده.

---

## ۳. سیستم طراحی جدید (Design Tokens)

همه در `global.css` به‌صورت CSS custom properties. هیچ رنگ/فونت/فاصله‌ای خارج از این توکن‌ها مجاز نیست.

### ۳.۱ رنگ — «کاغذ و جوهر» با یک لهجهٔ فیروزه‌ای

| نقش | توکن | مقدار | توضیح |
|---|---|---|---|
| پس‌زمینهٔ صفحه | `--paper` | `#FAFAF6` | کاغذ گرمِ خنثی (جای canvas سبزفام فعلی) |
| سطح (کارت) | `--surface` | `#FFFFFF` | |
| سطح فرورفته | `--surface-sunken` | `#F2F1EA` | نوارهای کد، بخش‌های خاکستری |
| جوهر اصلی | `--ink` | `#1D2125` | متن اصلی، تیترها |
| جوهر دوم | `--ink-2` | `#565D63` | متن توضیحی |
| جوهر سوم | `--ink-3` | `#878E94` | متادیتا، caption |
| خطوط | `--line` | `#E7E5DC` | border پیش‌فرض |
| خط پررنگ | `--line-strong` | `#C8C5B8` | hover، جداکنندهٔ مهم |
| **برند** | `--brand` | `#0B7A6B` | فیروزه‌ای هماهنگ با لوگو؛ لینک، فوکوس، اکشن اصلی |
| برند پررنگ | `--brand-strong` | `#085D51` | hover دکمهٔ اصلی |
| برند ملایم | `--brand-soft` | `#E4F2EF` | پس‌زمینهٔ hover آیتم‌ها، chip فعال ملایم |
| روی برند | `--on-brand` | `#FFFFFF` | |
| خطر | `--danger` | `#B3261E` | فقط پیام خطا |
| هشدار «موجود نیست» | `--notice-ink / --notice-bg` | `#7A4A08 / #FAF0DA` | **یک** کهربایی برای fa و en |
| شب (فقط cover و 404) | `--night / --night-ink` | `#14181B / #F4F4EE` | |

قواعد:
- بنفش (`--color-research*`) و طلایی (`--color-signature*`, `--color-gold*`) و سرمه‌ای‌های `navy` **حذف** می‌شوند. سرمه‌ای فقط به‌شکل `--night` برای cover/404 می‌ماند.
- کنتراست: متن اصلی روی paper ≥ 12:1، `ink-2` ≥ 5:1، `ink-3` فقط برای متادیتای غیرحیاتی (≥ 3.5:1). برند روی paper برای متن ≥ 4.5:1 (تست شود؛ در صورت نیاز `--brand-strong` برای متن لینک).
- `theme-color` همهٔ صفحات = `#FAFAF6` (شب = `#14181B`).

### ۳.۲ تایپوگرافی

| نقش | fa | en |
|---|---|---|
| بدنه | `Vazirmatn Variable` | `Inter Variable` |
| وزن بدنه | 400 (مهم: 450) | 400 |
| ارتفاع خط بدنه | 1.95 | 1.7 |
| اندازهٔ بدنه | 1.0625rem (17px) | 1rem |

- **اصلاح فوری باگ فونت:** استک‌ها به `"Vazirmatn Variable"` و `"Inter Variable"` تغییر می‌کنند (`global.css:61-65`). این تک خط، بیشترین تأثیر بصری را روی کل سایت دارد.
- مقیاس: `--text-display: clamp(2.1rem, 1.5rem + 2.2vw, 3rem)` وزن 700 / `h1: 1.875rem` / `h2: 1.375rem` / `h3: 1.125rem` / `meta: 0.875rem` / `kicker: 0.8125rem`.
- تیترها: fa وزن 700 با `line-height: 1.4`؛ en وزن 650، `letter-spacing: -0.01em`. حرف‌فاصلهٔ منفی روی fa **ممنوع**.
- kicker (برچسب بالای تیتر): رنگ برند، وزن 600؛ `letter-spacing` و uppercase فقط برای en؛ برای fa هیچ uppercase/mono-juggling.
- عرض سطر متن: متن بلند ≤ `66ch`؛ کاتالوگ‌ها و لیست‌ها ≤ `44rem`؛ فقط صفحهٔ اصلی و About تا `72rem` باز می‌شوند.
- اعداد و تاریخ fa همیشه با ارقام فارسی (`Intl` با `fa-IR-u-ca-persian`) — در زمان build، بدون JS.

### ۳.۳ فاصله، شعاع، سایه، حرکت

- فاصله (پایهٔ 4px): `--space-1:4 · 2:8 · 3:12 · 4:16 · 5:24 · 6:32 · 7:48 · 8:64 · 9:96`. ریتم عمودی بخش‌ها فقط از این مقیاس.
- شعاع: `--radius-sm:6 · --radius-md:10 · --radius-lg:14 · --radius-pill:999`. (xs/xl حذف.)
- سایه: **فلسفهٔ «خط بر سایه»** — عمق با border و کنتراست ساخته می‌شود. فقط یک سایه: `--shadow-1: 0 1px 2px rgb(20 24 27 / .05)` برای المان‌های شناور (dropdown، dialog).
- حرکت: فقط `transition` روی رنگ/border/opacity/transform با `160ms ease-out` (hover/focus) و `240ms` برای باز/بسته‌شدن. بدون پارالاکس، بدون انیمیشن ورود صفحه، بدون keyframe تزئینی. `prefers-reduced-motion: reduce` ⇒ همهٔ transitionها خاموش.

---

## ۴. زبان کامپوننت مشترک (تنها منبع مجاز UI)

همه در `global.css` (لایهٔ components) یا کامپوننت‌های Astro مشترک. کلاس‌های یک‌بارمصرف صفحه‌ای حذف می‌شوند.

### ۴.۱ دکمه‌ها — یک سیستم
```
.btn                پایه: min-height 44px، padding-inline 1.25rem، radius-md، وزن 600، فونت بدنه
.btn--primary       پس‌زمینه brand، متن on-brand؛ hover: brand-strong
.btn--ghost         شفاف، border line-strong، متن ink؛ hover: border brand + متن brand
.btn--quiet         فقط متن brand با underline-offset؛ برای اکشن‌های ثانویه
.btn--sm            نسخهٔ 36px برای جاهای فشرده (toolbar، چیپ کنار کارت)
```
- چیپ‌های سرمه‌ای «مشاهده» در کاتالوگ‌ها ⇒ `.btn--quiet` با آیکون فلش جهت‌دار (RTL-aware). دکمه داخل کارت شناور نمی‌ماند؛ کل کارت کلیک‌پذیر است (link stretch) و دکمه فقط نشانهٔ بصری است.

### ۴.۲ کارت و ردیف — دو الگو، نه شش
```
.card               سطح سفید، border line، radius-lg؛ hover: border brand + --shadow-1
.card--row          چیدمان افقی (متن + متادیتا) برای کاتالوگ‌ها؛ در موبایل عمودی
.list-row           ردیف زیرخط‌دار سبک (برای نوشته‌ها و لیست‌های متنی)
```
- داخل کارت: `kicker` (نوع/دسته) + عنوان + خلاصهٔ ۲ خطی + `meta-row` (تاریخ · زمان مطالعه · برچسب‌ها).
- حاشیهٔ رنگی کنار کارت‌ها (Landing/About) حذف؛ سلسله‌مراتب با تیتر و فاصله.

### ۴.۳ برچسب، چیپ، متادیتا
```
.kicker             برچسب بالای تیتر بخش/کارت (رنگ brand)
.chip               قرصِ border-line؛ .chip--active پرِ brand با متن سفید
.tag                لینک برچسب مقاله — همان خانوادهٔ chip ولی quiet (بدون کادر، رنگ ink-2، hover brand)
.meta-row           سطر 0.875rem به رنگ ink-3، جداکنندهٔ «·»
```
- برچسب دسته‌بندی همیشه به زبان همان صفحه و از دیکشنری محتواست؛ `project_type` خام CMS هرگز مستقیم رندر نمی‌شود.

### ۴.۴ حالت خالی — یک الگو
```
.empty-state        کارت sunken با عنوان کوتاه + یک جمله + یک اقدام (.btn--quiet)
```
- سه الگوی فعلی به همین یکی تبدیل می‌شوند. متن‌ها از دیکشنری محتوا (fa/en).

### ۴.۵ اسکلت صفحه
```
.container          max-width: var(--measure, 44rem)؛ padding-inline: clamp(1rem, 5vw, 2.5rem)
.section            فاصلهٔ عمودی --space-8؛ .section__head شامل kicker + h2 + لینک اختیاری
.prose              متن بلند: 66ch، فاصلهٔ پاراگراف --space-5، لینک‌ها brand با underline
```
- Breadcrumbs روی همهٔ صفحات غیر از خانه/cover؛ یک الگوی واحد.

---

## ۵. پوستهٔ سایت (Shell)

### ۵.۱ Header
- ارتفاع 64px، پس‌زمینهٔ `paper` با `border-bottom: 1px solid var(--line)` (glass حذف — سادگی و ثبات).
- راست (در RTL): لوگو + نام. چپ: ناوبری + دکمهٔ زبان.
- ناوبری: درباره، پژوهش، پروژه‌ها، نوشته‌ها، رزومه، تماس، جستجو. آیتم‌های کاتالوگ (انتشارات/کتاب‌ها/سخنرانی‌ها/دانلودها) در منوی «بیشتر» با `<details>` می‌مانند (no-JS).
- حالت فعال: علاوه بر مسیر دقیق، **بخش والد** هم active می‌شود (`/fa/research/topics/x` ⇒ «پژوهش» فعال).
- دکمهٔ زبان = `.btn--ghost .btn--sm` با متن «English»/«فارسی» — همان زبان دکمه‌ای که همه‌جا دیده می‌شود.

### ۵.۲ Footer
- سه ناحیه: ناوبری کوتاه، اطلاعات تماس (CMS)، زبان. پس‌زمینهٔ `--surface-sunken` با border-top.
- استایل فرم مردهٔ `.contact-form` حذف. لینک‌ها `.btn--quiet`-مانند، min-height 44px.

### ۵.۳ صفحهٔ ورود زبان (cover) — اتصال به هویت سایت
- می‌ماند (کارکردش درست است) اما با **همان سیستم**: فونت‌های سایت، `.btn`، توکن‌ها. پس‌زمینهٔ `--night`، نام دوجهانه با `--night-ink`، دو دکمهٔ `.btn--primary` (فیروزه‌ای) برای «فارسی» و `.btn--ghost` (روشن) برای «English» — نه فیروزه‌ای/طلایی دوگانهٔ فعلی.
- پس‌زمینهٔ تزئینی constellation حذف؛ به‌جایش یک خط افقی برند زیر نام (نشانهٔ امضای برند در کل سایت: خط 2px برند زیر تیترهای سطح یک).
- `theme-color` = `#14181B`. 404 هم همین قالب cover را با پیام کوتاه و لینک بازگشت استفاده می‌کند.

### ۵.۴ صفحهٔ اصلی (Landing)
- حذف: SVG متحرک constellation و آیلند `Constellation3D` (Three.js ~600KB فقط برای دکور). **هیرو = تایپوگرافی**: kicker، نام بزرگ، یک جملهٔ جایگاه‌یابی، دو دکمه (نمونه‌کارها / درباره)، خط برند زیر تیتر.
- بخش‌ها با `.section` و `.section__head`: «زمینه‌های تمرکز» (۳ کارت `.card`)، «شواهد» (لیست متنی)، «نوشته‌های تازه» (۳ `.list-row` + لینک «همه»)، CTA تماس.
- کد Landing از ۵۳۴ خط استایل به < ۱۵۰ خط می‌رسد چون از کامپوننت‌های مشترک استفاده می‌کند.

---

## ۶. تصمیم دربارهٔ گراف روابط: **حذف کامل**

گراف خوانا نیست، کنتراست ندارد، با JS است و همان اطلاعات را دوبار نشان می‌دهد. تا وقتی یک طراحی معنادار جایگزین شود، حذف می‌شود:

1. حذف `components/research/ResearchGraphSection.astro`، `ResearchGraphIsland.tsx`، `lib/cms/research-graph.ts`.
2. حذف import و استفاده در `pages/{fa,en}/research/index.astro` (خطوط import و `{graph ? … : null}`).
3. حذف کلیدهای `research.graph*` از `data/content.ts`.
4. حذف `qa/research-graph.spec.mjs`.
5. حذف وابستگی `gsap` از `package.json` (الان هم بی‌استفاده است).
6. جایگزین سبک در صفحهٔ پژوهش: یک بخش «مسیرهای پژوهش» با چیپ‌های موضوعات (`.chip` لینک‌دار به `/research/topics/<slug>`) — فقط CSS، خوانا، و همان پیوند موضوع↔پروژه↔انتشارات در صفحهٔ خودِ موضوع دیده می‌شود.

---

## ۷. بازنویسی صفحه‌به‌صفحه

| صفحه | تغییرات کلیدی |
|---|---|
| **cover (index)** | §۵.۳ — هم‌سیستمی با سایت، حذف constellation، دکمه‌های `.btn` |
| **خانه** | §۵.۴ — هیرو تایپوگرافیک، حذف Three.js/SVG متحرک، بخش‌ها با `.section` |
| **پژوهش (index)** | حذف گراف (§۶)؛ معرفی کوتاه + چیپ‌های موضوعات + کاتالوگ با `.card--row`؛ تاریخ جلالی |
| **موضوع پژوهش (detail)** | قالب `.prose` + `meta-row`؛ پیوند به پروژه‌ها/انتشارات مرتبط به‌صورت لیست |
| **پروژه‌ها** | همان کاتالوگ واحد؛ badge نوع پروژه **ترجمه‌شده** از دیکشنری؛ فیلتر/مرتب‌سازی با اسکریپت مشترک واحد (نه دو کپی) |
| **جزئیات پروژه** | `.prose` + گالری Lightbox موجود (استایل هم‌توکن)؛ DemoEmbed بدون تغییر رفتار |
| **نوشته‌ها** | `.list-row`ها با تاریخ جلالی + `.tag`؛ صفحهٔ مقاله: `.prose` با تیتربندی یکنواخت، StoryBody بدون استایل فیزیکی border |
| **رزومه و CV** | تب‌ها با radio-CSS موجود ولی استایل هم‌سیستم (chip)؛ تولبار sticky با `--space-sticky-offset` (رفع باگ هم‌پوشانی)؛ بخش دانلودها با `.card--row` و دکمهٔ `.btn--primary` |
| **تماس** | برچسب‌های «ایمیل/لینکدین/ORCID/محل کار/موقعیت» به زبان صفحه؛ فرم با `.btn--primary` و فیلدهای هم‌توکن؛ پیام «به‌ایمیل پاسخ می‌دهم» حفظ |
| **جستجو** | Pagefind با تم توکن‌شده؛ noscript fallback با `.empty-state`؛ عرض 44rem مثل بقیه |
| **درباره** | تب‌ها/چیپ‌ها با سیستم مشترک؛ کارت‌های entry با `.card`؛ حذف استایل‌های یک‌بارمصرف (~۴۰۰ خط → <۱۵۰) |
| **انتشارات/کتاب‌ها/سخنرانی‌ها/دانلودها/آموزش/خلق‌آفرینی (کاتالوگ‌ها)** | همه از **یک کامپوننت مشترک `CatalogPage`** استفاده می‌کنند (پایان ۲۰ نسخهٔ کپی)؛ empty-state واحد؛ تاریخ هم‌قالب |
| **صفحات detail کاتالوگ** | قالب مشترک: breadcrumb + h1 + `meta-row` + `.prose` + citation block با فونت mono واقعی؛ برچسب‌های «DOI/استناد/PDF» ترجمه‌شده |
| **404** | قالب cover کوتاه: پیام + دکمهٔ بازگشت به خانه |

---

## ۸. قواعد دوجهانگی (اجرایی)

1. **تاریخ:** تابع مشترک `formatDate(date, locale)` در `src/lib/format.ts` — fa: `Intl.DateTimeFormat('fa-IR-u-ca-persian')` (جلالی + ارقام فارسی)، en: `en-IE` یا `en-US` (میلادی). همهٔ رندرهای `iso.slice(0,10)` و رشته‌های خام حذف. مقدار fallback هاردکد `"2026-08-16"` در Downloads حذف.
2. **ارقام:** در fa، شمارنده‌ها/تاریخ‌ها با ارقام فارسی؛ کدها و DOI لاتین می‌مانند.
3. **برچسب‌ها:** هیچ رشتهٔ UI انگلیسی روی صفحات fa (و برعکس) — همه از دیکشنری `data/content.ts`.
4. **RTL:** فقط logical properties؛ باقی‌مانده‌های فیزیکی (`border-bottom` در Header/ArticleCard/SeriesNav/StoryBody) به `border-block-end` تبدیل می‌شوند.
5. **فونت:** استک‌های Variable اصلاح (§۳.۲)؛ `font-feature-settings` پیش‌فرض؛ هیچ letter-spacing منفی روی fa.

---

## ۹. پاک‌سازی (همراه بازطراحی، الزامی)

- حذف `gsap` از وابستگی‌ها.
- حذف Tailwind import از `global.css` (فقط ۲ کلاس utility استفاده می‌شود → معادل دستی در BaseLayout).
- حذف توکن‌های مرده و استایل فرم مردهٔ Footer.
- ادغام دو اسکریپت فیلتر کاتالوگ (research/projects) در یک اسکریپت مشترک.
- حذف `Constellation3D.tsx` و وابستگی `three`.
- یکسان‌سازی رنگ notice (کهربایی واحد fa/en).

---

## ۱۰. فازهای اجرا و معیار پذیرش

> ترتیب طوری است که هر فاز خروجی قابل‌دیدن و قابل‌تست دارد. هیچ فازی بدون رعایت «محدودیت‌های سخت» §۱۲ شروع نمی‌شود.

### فاز ۰ — شالوده (بدون تغییر ظاهرِ صفحات)
- توکن‌های جدید در `global.css`، اصلاح نام فونت‌ها، `formatDate`، حذف کدهای مرده (§۹).
- ✅ پذیرش: build سبز؛ فونت‌ها در DevTools با نام Variable لود می‌شوند؛ هیچ صفحه‌ای خراب نشده.

### فاز ۱ — سیستم کامپوننت و شل
- `.btn/.card/.chip/.kicker/.meta-row/.empty-state/.container/.section/.prose` در global.css؛ بازنویسی Header/Footer/Breadcrumbs؛ cover و 404.
- ✅ پذیرش: همهٔ دکمه‌ها/لینک‌های شل از `.btn` استفاده می‌کنند؛ active state بخش والد کار می‌کند؛ cover هم‌خانوادهٔ سایت است.

### فاز ۲ — کاتالوگ‌ها و جزئیات
- کامپوننت مشترک `CatalogPage`؛ مهاجرت ۸ کاتالوگ + جزئیات؛ حذف ۲۰ بلوک کپی؛ تاریخ/برچسب‌ها اصلاح.
- ✅ پذیرش: grep نشان می‌دهد فقط یک تعریف از استایل کاتالوگ وجود دارد؛ هیچ تاریخ ISO خام روی fa نیست.

### فاز ۳ — خانه، پژوهش، پروژه‌ها، نوشته‌ها
- بازنویسی Landing (حذف Three.js)، صفحهٔ پژوهش (حذف گراف + چیپ موضوعات)، پروژه‌ها، نوشته‌ها.
- ✅ پذیرش: هیچ آیلند روی صفحات عمومی جز Lightbox/Pagefind نمانده باشد؛ وزن JS صفحهٔ اصلی < ۵KB.

### فاز ۴ — About، CV، تماس، جستجو + پولیش
- ✅ پذیرش: چک‌لیست §۱۱ کامل تیک بخورد.

---

## ۱۱. چک‌لیست پذیرش نهایی (هر صفحه، هر دو زبان)

- [ ] فقط رنگ/فونت/فاصلهٔ توکن؛ grep برای `#[0-9a-fA-F]{3,6}` داخل صفحات = فقط global.css
- [ ] کنتراست متن ≥ 4.5:1؛ متادیتا ≥ 3.5:1؛ فوکوس‌رینگ روی همهٔ تعاملی‌ها
- [ ] بدون JS: ناوبری، خواندن، فیلترهای `<details>`، فرم تماس کار می‌کند
- [ ] `prefers-reduced-motion` ⇒ بدون انیمیشن
- [ ] 375px / 768px / 1440px سالم؛ RTL و LTR هر دو
- [ ] تاریخ‌ها: fa جلالی با ارقام فارسی، en میلادی؛ بدون رشتهٔ ISO خام
- [ ] هیچ برچسب زبانِ اشتباه روی هیچ صفحه‌ای نیست
- [ ] حالت خالی هر بخش: پیام + اقدام، با الگوی واحد
- [ ] وزن صفحه: بدون Three.js/motion/gsap؛ فونت‌ها فقط subset لازم
- [ ] مسیرها، اسلاگ‌ها، محتوا و سئو تغییر نکرده‌اند (فقط ظاهر)

---

## ۱۲. محدودیت‌های سخت (شکننده — دست نزن)

- معماری: Astro استاتیک، خروجی `apps/web`، بدون JS اجباری در صفحات عمومی؛ React فقط island.
- محتوا از CMS می‌آید؛ مدل دیتا، اسلاگ‌ها، مسیرها و redirectها دست‌نخورده.
- حفظ کامل محتوا: هیچ متن/ترجمه/فایلی حذف نمی‌شود؛ فقط نحوهٔ نمایش عوض می‌شود.
- هر تغییر واقعی، ورک‌لاگ خودش را می‌گیرد.

---

## ۱۳. ممنوعیت‌ها (Anti-patterns — از این به بعد)

- دکمه/کارت/چیپ/empty-state دست‌ساز خارج از سیستم مشترک.
- رنگ hex داخل کامپوننت یا صفحه.
- انیمیشن تزئینی، پارالاکس، صحنهٔ سه‌بعدی، ذرات و ستاره.
- برچسب انگلیسی روی صفحهٔ فارسی (و برعکس).
- تاریخ خام ISO روی صفحات فارسی.
- کپی‌کردن بلوک استایل بین صفحات به‌جای کامپوننت مشترک.
- uppercase/letter-spacing روی متن فارسی.
